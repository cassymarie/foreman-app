<h2 id="sub_header"> <%= link_to @jobsite.name, jobsite_path(@jobsite) %> </h2>
<div class="row">
    <div class="col-md-3">
        <div class="jobsite_form" >
            <%= render "form" %> <%# Add/Edit Job Form %>
        </div>

        <div class="jobsite_form" >
            <%= render "form_area" %> <%# Add/Edit Areas Form %>
        </div>
    </div>

    <div class="col-md-9">
    <div class="jobsite_form">
        <h2 id="form_header">Jobs</h2> 
            <div class="container-fluid">
                <button class="btn btn-primary btn-sm"> Hours </button>
                <button class="btn btn-primary btn-sm"> Employees </button>
            </div>
        <%= render partial: "jobsites/collection_table", locals: {jobsite: @jobsite, collection: @jobsite.jobs, view_by: 'none', show: 'jobs', editable: true, customer: true, job_areas: true} %>
    </div>
    </div>
</div>