    <div class="row">
        <div class="col-md-2">
            <div class="jobsite_form" >
                <%= render "form" %> <%# Add/Edit Job Form %>
            </div>
        </div>

        <div class="col-md-10">
            <div class="jobsite_form">
                <h2 id="form_header"><%= @jobsite.name %></h2> 
                <%= render partial: "jobsites/jobsite_employees", locals: {jobsite: @jobsite} %>
            </div>
        </div>
    </div>
